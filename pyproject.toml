[project]
name = "enterprise-supply-chain-and-package-tracking-data-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.15.0",
    "pyspark>=4.2.0",
]
